---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: deployruns.maxicloud.maximum.vc
spec:
  group: maxicloud.maximum.vc
  names:
    kind: DeployRun
    listKind: DeployRunList
    plural: deployruns
    singular: deployrun
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.sha
      name: SHA
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DeployRun is the Schema for the deployruns API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DeployRunSpec defines the desired state of DeployRun
            properties:
              applicationName:
                description: ApplicationName is the name of the Application CR to
                  deploy.
                type: string
              owner:
                description: Owner is the GitHub repository owner (org or user).
                type: string
              prNumber:
                description: PRNumber is the pull request number. Set only for PR-triggered
                  deployments.
                type: integer
              repo:
                description: Repo is the GitHub repository name.
                type: string
              sha:
                description: SHA is the commit SHA to build and deploy.
                type: string
            required:
            - applicationName
            - owner
            - repo
            - sha
            type: object
          status:
            description: DeployRunStatus defines the observed state of DeployRun.
            properties:
              buildRunRef:
                description: BuildRunRef is the name of the BuildRun CR created for
                  this pipeline.
                type: string
              checkRunID:
                description: CheckRunID is the GitHub Check Run ID created for this
                  pipeline.
                format: int64
                type: integer
              conditions:
                description: Conditions contains the conditions for the DeployRun.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              deploymentSummaryCommentID:
                description: DeploymentSummaryCommentID is the GitHub PR comment ID
                  created for this pipeline.
                format: int64
                type: integer
              finishedAt:
                description: FinishedAt is when the pipeline completed.
                format: date-time
                type: string
              image:
                description: Image is the built container image reference, set after
                  build succeeds.
                type: string
              phase:
                description: Phase is the current lifecycle phase of the pipeline.
                enum:
                - Queued
                - Building
                - Deploying
                - Succeeded
                - Failed
                type: string
              startedAt:
                description: StartedAt is when the pipeline started.
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
